.container
    div(ng-controller="QuestionsAdminController")
        p.text.text-info.text-size-18(ng-show="hasQustions()") Няма въпроси!
        div.table-responsive.margin-top-15(ng-hide="hasQustions()")
            table.table.table-bordered.table-hover
                thead
                    tr
                        th Заглавие
                        th Автор
                        th Дата
                        th Действия
                .tbody
                    tr(ng-repeat="question in questions")
                        td
                            a(href="#/questions/{{question.id}}") {{ question.title }}
                        td
                            a(href="#/users/{{ question.author.id }}") {{ question.author.username }}
                        td {{ question.date }}
                        td
                            btn.btn.btn-primary.btn-sm.margin-right(type="button", ng-click="setQuestionId(question.id)" title="Delete", data-toggle="modal", data-target="#deleteModal")
                                i.glyphicon.glyphicon-remove
            div#deleteModal.modal(tabindex="-1", role="dialog", aria-hidden="true")
                div.modal-dialog
                    div.modal-content
                        div.modal-header
                            button.close(type="button", data-dismiss="modal")
                                span(aria-hidden="true") &times;
                                span.sr-only Close
                            h4.modal-title Сигурни ли сте, че искате да изтриете въпроса?
                        div.modal-footer
                            button.btn.btn-primary(type="button", ng-click="deleteQuestion()", data-dismiss="modal") Да
                            button.btn.btn-default(data-dismiss="modal") Не