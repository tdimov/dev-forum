div.panel.panel-default
    div.panel-heading
        h4 Tags
    div.panel-body
        ul.list-inline(ng-controller="TagsController")
            li.label.label-primary.margin-right(ng-repeat="tag in tags")
                a.text-white.not-hover(href="#/questionsByTag/{{tag.name}}") {{ tag.name }}
    div.panel-footer
        a(href="#/tags") See all...